1. The CI check `check-requirements` failed because "PR must reference an issue".
2. Looking at the error log, the regex it uses to find issue references is:
   `const issuePattern = /(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)?\s*#(\d+)/gi;`
   It extracts issue numbers from both the PR title and the PR body.
3. My commit message was:
   ```
   feat: Add cost feedback to CLI outputs

   Resolves #2505
   ```
   Wait, the PR description markdown file I generated was:
   ```
   Resolves #2505
   ```
   But when creating the PR, the title I used was `[Feature] Cost Feedback` or `feat: output cost feedback to CLI`, and the body used `.pr-2505.md`. I should double check what the script in `.github/workflows/` expects. Oh, the script is right there in the logs!

   ```javascript
   const pr = context.payload.pull_request;
   const prNumber = pr.number;
   const prBody = pr.body || '';
   const prTitle = pr.title || '';

   // ...
   const issuePattern = /(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)?\s*#(\d+)/gi;

   const allText = `${prTitle} ${prBody}`;
   const matches = [...allText.matchAll(issuePattern)];
   const issueNumbers = [...new Set(matches.map(m => parseInt(m[1], 10)))];
   ```

   The script failed with `Found issue references: none`. Why did it not find `Resolves #2505` in my PR body?
   Ah, maybe I didn't actually create the PR correctly with the body from `.pr-2505.md`. Wait, `commit_pr.py` failed because it couldn't find `~/.git-credentials`! So I didn't create the PR using the python script, I instead just called the `submit` tool.
   When calling `submit`, I provided the `description` parameter. Does the `submit` tool map the `description` parameter directly to the PR body? Yes.
   My description in the `submit` tool was:
   ```
   Implemented a new Cost Feedback feature allowing developers to view real-time cost breakdown per node and per model during agent execution via the CLI (hive run and hive shell).

   - Track cost per stream event or LLM complete call in Litellm Provider abstraction via Litellm's helper.
   - Passed total cost and token counts directly through NodeResult and ExecutionResult along the GraphExecutor path, including within parallel branch execution nodes.
   - Reformatted Output text of CLI to mirror precisely the requested format (i.e "TOTAL", "COSTS: BY STEPS", "COST OF MODELS USED", "TOKENS").
   ```
   Oh, I didn't include `Resolves #2505` in the `description` argument of the `submit` tool. I only included it in the `commit_message`.
   So the PR body generated by the `submit` tool didn't have `Resolves #2505`.

4. I need to run `submit` again with `Resolves #2505` in the `description` parameter.
