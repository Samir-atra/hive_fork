{
  "mcpServers": {
    "hive-tools": {
      "command": "uv",
      "args": ["run", "python", "mcp_server.py", "--stdio"],
      "env": {
        "PYTHONPATH": "${PWD}/../"
      },
      "cwd": "tools"
    }
  }
}
